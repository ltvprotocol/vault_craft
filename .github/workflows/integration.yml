name: Integration CI

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  integration_test:
    if: github.repository == 'ltvprotocol/vault_craft'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly-567ac47f2b4bf6e295c9ab876a781c563da7b3f1

      - name: Print forge version
        run: forge --version

      - name: Run tests
        run: forge build && forge test -vvv --match-path "test/FlashLoanHelperWstethAndWeth.sol"
        env:
          RPC_MAINNET: ${{ secrets.RPC_MAINNET }}
